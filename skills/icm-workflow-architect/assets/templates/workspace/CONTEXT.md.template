# {Workspace Name}

The flow in one line: {stage 1} -> {stage 2} -> {final deliverable}.

| Stage | Job | Input | Output | Human check |
|---|---|---|---|---|
| `01-{name}` | {five words} | {working input} | `output/{run-slug}-{artifact}.md` | {decision} |
| `02-{name}` | {five words} | Stage 01 output | `output/{run-slug}-{artifact}.md` | {decision} |

Factory (stable every run): `_shared/` and stage `references/`.

Product (new each run): stage `output/` artifacts.

A stage is complete only when its required artifact or external receipt exists and passed the declared Human Check.
